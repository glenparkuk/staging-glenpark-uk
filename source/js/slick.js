(function($) { // reference query
	$(function() { // shorthand for onLoad()

		$(".js-slick-carousel").slick({
			fade: true,
			autoplay: true,
			arrows: false,
			autoplaySpeed: 5000
		});

	});
})(jQuery);